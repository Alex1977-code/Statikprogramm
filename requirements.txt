numpy>=1.24
scipy>=1.10
PySide6>=6.5
pyvista>=0.42
pyvistaqt>=0.11
zstandard>=0.21     # HiCAD-Archive (.sza/.kra/.fga) sind zstd-gepackt
# Mehrkern-Gleichungsloeser. Ohne ihn faellt der Loeser auf SuperLU zurueck,
# und SuperLU rechnet auf genau einem Kern - am Wuerfel mit 34.914 FHG
# 12,25 s statt 1,38 s. Die exe bringt MKL mit (siehe packaging/Statik3D.spec).
pypardiso>=0.4
mkl
# Zweiter Loeser in der exe: PyAMG (MIT, algebraisches Mehrgitter + CG) -
# iterativ und speicherarm; "Gleichungsloeser mitinstallieren" (13.09.2026).
pyamg>=5
# Dritter Loeser in der exe: MUMPS 5.8.2 (CeCILL-C, Lizenztext im Paket) als
# eigenes Windows-Rad aus packaging/ - gebaut mit gfortran/OpenMP, OpenBLAS
# und METIS (docs/MUMPS_Windows_Bauanleitung.md). --find-links gilt relativ
# zu dieser Datei; unter Linux gibt es das Rad nicht.
--find-links packaging
mumps==5.8.2 ; sys_platform == "win32"

# optional (keines davon in der exe - Lizenzen siehe Benutzerhandbuch, Kap. 9;
# gmsh, netgen-mesher und MMG3D laedt das Programm auf Wunsch selbst nach:
# Extras -> Vernetzer installieren..., statik3d/werkzeuge.py):
# gmsh>=4.11        CAD-Import (STEP/IGES/BREP/STL) und Vernetzer (GPL)
# netgen-mesher     Vernetzer Netgen (LGPL)
# scikit-sparse     CHOLMOD-Loeser (LGPL/GPL)
# scikit-umfpack    UMFPACK-Loeser (GPL)
# pymumps           MUMPS-Loeser (CeCILL-C)
# reportlab, svglib PDF-Bericht direkt aus dem Programm
# qrcode            QR-Code der Handy-Adresse beim Start von run_web.py
